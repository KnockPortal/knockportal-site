# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=          # SERVER ONLY — never NEXT_PUBLIC, never in client bundle

# Stripe
STRIPE_SECRET_KEY=sk_live_...       # SERVER ONLY
STRIPE_WEBHOOK_SECRET=whsec_...     # SERVER ONLY

# Resend (email — layer 3)
RESEND_API_KEY=re_...
RESEND_FROM=KnockPortal <noreply@knockportal.com>
